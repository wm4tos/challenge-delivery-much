version: '3'

services:
  api:
    container_name: delivery_much_challenge_api
    image: wricke/delivery_much_challenge
    ports:
      - "3000:3000"
    command: sh -c "npm install && npm run start:dev"
    working_dir: /app
    volumes:
      - ./:/app
    env_file:
      - ./.env