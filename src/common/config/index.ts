export const { PORT = 3000 } = process.env;
export const { NODE_ENV } = process.env;
export const { GIPHY_API_KEY } = process.env;
export const { RECIPE_PUPPY_URL } = process.env;
export const { GIPHY_API_URL } = process.env;
